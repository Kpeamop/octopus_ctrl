/*const task=require('./client/task');

t=new task.tasklist();

x=t.add('gnome-calculator',[]);

x.setenv('sss','ff');

x.run();

t.ev.exit=(x,obj) => { console.log(x,obj.runtime) ; };
// t.autoremove=true;
setTimeout(() => { t.killall(); console.log(t.count()); }, 1300);
*/
